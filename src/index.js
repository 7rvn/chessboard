import React from "react";
import ReactDOM from "react-dom";
import App from "./demos/LegalMovesOnly";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
